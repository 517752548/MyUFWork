package com.imop.lj.db.dao;

import java.util.List;

import com.imop.lj.core.orm.DBService;
import com.imop.lj.db.model.$entityName;

/**
 * ${comment}的DAO
 *
 */
public class $daoName extends BaseDao<$entityName>  {
	/** 查询语句名称 ：查询玩家的任务 */
	public static final String QUERY_TASK_BY_CHARID = "${queryName}";

	public $daoName(DBService dbService) {
		super(dbService);
	}

	@Override
	protected Class<$entityName> getEntityClass() {
		return ${entityName}.class;
	}
	
	public void saveOrUpdate($entityName entity) {
		dbService.saveOrUpdate(entity);
	}

	/**
	 * 查询玩家的所有目标任务
	 * 
	 * @return
	 */
	@SuppressWarnings({ "unchecked", "rawtypes" })
	public List<$entityName> loadEntityByCharId(long charId) {
		List _queryList = dbService.findByNamedQueryAndNamedParam(QUERY_TASK_BY_CHARID, 
				new String[]{ "charId" } , new Object[]{ charId });
		return _queryList; 
	}
	
}
