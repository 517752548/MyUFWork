package com.imop.lj.gameserver.cd;

import com.imop.lj.gameserver.human.Human;

/**
 * 冷却事件监听
 *
 * @author haijiang.jin
 *
 */
public interface CdListener {
	/**
	 * 增加冷却队列上限
	 *
	 * @param human
	 * @param addedCdQueue
	 *
	 */
	void afterAddCdQueueMax(Human human, CdQueue addedCdQueue);
}
