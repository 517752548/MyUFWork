package com.imop.lj.gameserver.goodactivity.useractivity.impl;

import com.imop.lj.gameserver.goodactivity.activity.impl.EveryCostActivity;
import com.imop.lj.gameserver.goodactivity.useractivity.AbstractUserGoodActivity;
import com.imop.lj.gameserver.goodactivity.userdatamodel.impl.EveryCostUserDataModel;

public class EveryCostUserGoodActivity extends AbstractUserGoodActivity {

	public EveryCostUserGoodActivity(long charId, EveryCostActivity activity) {
		super(charId, activity);
		
	}
	
	protected EveryCostUserDataModel buildUserDataModel() {
		return new EveryCostUserDataModel(this);
	}
	
}
