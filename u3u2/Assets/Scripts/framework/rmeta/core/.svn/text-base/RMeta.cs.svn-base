
public class RMeta
    {
        private static IConfig _config;

        public static void starUp(IConfig config)
        {
            _config = config;
        }

        public static IConfig config
        {
            get
            {
                return _config;
            }
        }
    }