
namespace app.net
{
	public class CommonItemData
	{
		/** 道具实例uuid */
		public string uuid;
		/** 包id */
		public int bagId;
		/** 道具在背包内位置索引 */
		public int index;
		/** 道具模板Id */
		public int tplId;
		/** 数量 */
		public int count;
		/** 最后一次更新时间 */
		public long lastUpdateTime;
		/** 剩余使用时限描述 */
		public string expireDesc;
		/** 持有者id，主背包为0 */
		public long wearerId;
		/** 绑定状态，0绑定，1未绑定 */
		public int bind;
		/** 道具props，json */
		public string props;
	}
}