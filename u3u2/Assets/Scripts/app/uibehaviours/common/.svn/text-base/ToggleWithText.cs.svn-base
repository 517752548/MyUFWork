using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class ToggleWithText : MonoBehaviour
{
    public GameUUToggle toggle;
    public Text toggleText;

    public void Init(GameUUToggle toggle, Text text)
    {
        this.toggle = toggle;
        this.toggleText = text;
    }
}
