using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace app.db
{
    public class NpcTemplateDB : NpcTemplateDBBase
    {
        // TODO 可能会自定义一些属性或方法

        /// <summary>
        /// 获得一个npc的模板数据
        /// </summary>
        /// <param name="npcId"></param>
        /// <returns></returns>
        /*
        public NpcTemplate GetNpcTmpByNpcId(int npcId)
        {
            foreach (KeyValuePair<int, NpcTemplate> pair  in idKeyDic)
            {
                if (pair.Key == npcId)
                {
                    return pair.Value;
                }
            }
            return null;
        }
        */
        
    }
}
