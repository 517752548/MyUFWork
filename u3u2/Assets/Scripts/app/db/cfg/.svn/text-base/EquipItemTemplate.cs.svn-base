using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace app.db
{
    public class EquipItemTemplate : EquipItemTemplateVO
    {
        //private Dictionary<int, int> addDic;

        ///// <summary>
        ///// 获取装备增加的二级属性字典
        ///// </summary>
        ///// <returns></returns>
        //public Dictionary<int, int> GetAddPropDic()
        //{
        //    if (this.addDic != null)
        //    {
        //        return this.addDic;
        //    }

        //    Dictionary<int, int> addDic = new Dictionary<int, int>();
        //    int c = propList.Count;
        //    for (int i = 0; i < c; i++ )
        //    {
        //        int k = int.Parse(propList[i].propKey);
        //        int v = propList[i].propValue;
        //        int curV = 0;
        //        if (addDic.ContainsKey(k))
        //        {
        //            addDic.TryGetValue(k, out curV);
        //            addDic.Remove(k);
        //        }
        //        addDic.Add(k, curV + v);
        //    }

        //    return addDic;
        //}

    }
}
