using UnityEngine;
using System.Collections;

public class xiaoshi : MonoBehaviour {
	public float dietime;
	public float b;
	// Use this for initialization
	void Start () {
//		Invoke (jianyin, dietime);
	}

	public  void jianyin ()
	{
		b = this.gameObject.GetComponent<ParticleSystemRenderer> ().material.color.a;
		b--;
	}
}