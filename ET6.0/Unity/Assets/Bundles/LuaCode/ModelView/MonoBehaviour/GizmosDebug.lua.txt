-- Generated by CSharp.lua Compiler
local System = System
local UnityEngine = UnityEngine
System.namespace("ET", function (namespace)
  namespace.class("GizmosDebug", function (namespace)
    local Awake, OnDrawGizmos, class
    Awake = function (this)
      class.Instance = this
    end
    OnDrawGizmos = function (this)
      if #this.Path < 2 then
        return
      end
      for i = 0, #this.Path - 1 - 1 do
        UnityEngine.Gizmos.DrawLine(this.Path:get(i), this.Path:get(i + 1))
      end
    end
    class = {
      base = function (out)
        return {
          out.UnityEngine.MonoBehaviour
        }
      end,
      Awake = Awake,
      OnDrawGizmos = OnDrawGizmos
    }
    return class
  end)
end)
