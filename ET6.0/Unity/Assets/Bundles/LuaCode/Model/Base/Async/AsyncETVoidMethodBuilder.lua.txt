-- Generated by CSharp.lua Compiler
local System = System
local ET
System.import(function (out)
  ET = out.ET
end)
System.namespace("ET", function (namespace)
  namespace.struct("AsyncETVoidMethodBuilder", function (namespace)
    local Create, getTask, SetException, SetResult, AwaitOnCompleted, AwaitUnsafeOnCompleted, Start, SetStateMachine, 
    class
    Create = function ()
      local builder = class()
      return builder:__clone__()
    end
    getTask = function (this)
      return System.default(ET.ETVoid)
    end
    SetException = function (this, exception)
      ET.Log.Error1(exception)
    end
    SetResult = function (this)
      -- do nothing
    end
    AwaitOnCompleted = function (this, awaiter, stateMachine, TAwaiter, TStateMachine)
      awaiter:OnCompleted(System.fn(stateMachine, stateMachine.MoveNext))
      return awaiter, stateMachine
    end
    AwaitUnsafeOnCompleted = function (this, awaiter, stateMachine, TAwaiter, TStateMachine)
      awaiter:UnsafeOnCompleted(System.fn(stateMachine, stateMachine.MoveNext))
      return awaiter, stateMachine
    end
    Start = function (this, stateMachine, TStateMachine)
      stateMachine:MoveNext()
      return stateMachine
    end
    SetStateMachine = function (this, stateMachine)
    end
    class = {
      Create = Create,
      getTask = getTask,
      SetException = SetException,
      SetResult = SetResult,
      AwaitOnCompleted = AwaitOnCompleted,
      AwaitUnsafeOnCompleted = AwaitUnsafeOnCompleted,
      Start = Start,
      SetStateMachine = SetStateMachine,
      __metadata__ = function (out)
        return {
          methods = {
            { "AwaitOnCompleted", 0x20206, AwaitOnCompleted, function (TAwaiter, TStateMachine) return TAwaiter, TStateMachine end, System.DebuggerHiddenAttribute() },
            { "AwaitUnsafeOnCompleted", 0x20206, AwaitUnsafeOnCompleted, function (TAwaiter, TStateMachine) return TAwaiter, TStateMachine end, System.DebuggerHiddenAttribute(), System.Security.SecuritySafeCriticalAttribute() },
            { "Create", 0x8E, Create, class, System.DebuggerHiddenAttribute() },
            { "SetException", 0x106, SetException, System.Exception, System.DebuggerHiddenAttribute() },
            { "SetResult", 0x6, SetResult, System.DebuggerHiddenAttribute() },
            { "SetStateMachine", 0x106, SetStateMachine, System.IAsyncStateMachine, System.DebuggerHiddenAttribute() },
            { "Start", 0x10106, Start, function (TStateMachine) return TStateMachine end, System.DebuggerHiddenAttribute() }
          },
          properties = {
            { "Task", 0x206, out.ET.ETVoid, getTask, System.DebuggerHiddenAttribute() }
          }
        }
      end
    }
    return class
  end)
end)
