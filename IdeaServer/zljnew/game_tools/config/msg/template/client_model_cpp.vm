#macro( smallType $type)
#if($type == "Byte")char#elseif( $type == "String" )string#elseif( $type == "Boolean" )bool#elseif( $type == "Short" )short#elseif( $type == "Integer" )int32#elseif( $type == "Long" )int64#else$type *#end
#end

#ifndef ${field.clientType}_H
\#define ${field.clientType}_H

\#include "type.h"
#foreach( $typeName in $field.subFieldTypeNames )
\#include "${typeName}.h"
#end

class $field.clientType
{
public:
#foreach( $subField in $field.subFields )
	/** $subField.comment */
#if($subField.list)
	vector <#smallType($subField.clientType)> * ${subField.smallName};
#else
	#smallType($subField.clientType) ${subField.smallName};
#end
#end
};

#endif
