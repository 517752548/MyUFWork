package com.imop.lj.core.constants;

import org.apache.mina.core.session.IoSession;
import org.apache.mina.filter.codec.ProtocolDecoder;

import com.imop.lj.core.session.ISession;

/**
 * 在为Mina的{@link IoSession#setAttribute(Object, Object)}设置属性时使用的key
 * 
 * @author dan.shan
 * 
 */
public enum SessionAttrKey {
	/** 用于设置{@link ISession} */
	ISESSION,
	/** 用于设置Session对应的{@link ProtocolDecoder} */
	DECODER;
}
